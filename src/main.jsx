import React from "react";
import ReactDOM from "react-dom/client";
import GateEceDashboard from "./GateEceDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GateEceDashboard />
  </React.StrictMode>
);
